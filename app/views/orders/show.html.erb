<p id="notice"><%= notice %></p>

<p>
  <strong>Matches:</strong>

  <%= @number_of_matches %>
</p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order.chests.split do |chest| %>
      <tr>
        <td><%= chest %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
