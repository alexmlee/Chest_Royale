<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <% @order.chests.each_with_index do |chest, index| %>
    <% fields_for 'chest[]', chest do |chest_fields| %>
      
      <%= chest_fields.text_field index %>
    <% end %>
  <% end %>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
